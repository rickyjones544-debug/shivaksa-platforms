// TODO: Implement agent layout with sidebar and navigation
export const dynamic = 'force-dynamic';

export default function AgentLayout({ children }: { children: React.ReactNode }) {
  return <div>{children}</div>;
}
